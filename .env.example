# 拷贝到 .env 后填真实值; .env 已在 .gitignore, 不会被 push

# 钱包解密密码 (跑 daemon 时用, 单次手动跑可交互输入)
RHO_PASSWORD=your-password-here

# Infura API key 池, 多个用逗号分隔; 不填则用 publicnode 兜底
# 一个 key 限 100k req/天, 1000 钱包跑一天约 ~10k req, 单 key 也够
INFURA_KEYS=key1,key2,key3,key4,key5,key6,key7,key8
